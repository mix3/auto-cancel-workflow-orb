version: 2.1

orbs:
  auto-cancel:
    executors:
      default:
        docker:
          - image: circleci/buildpack-deps:latest
    jobs:
      auto-cancel:
        executor: default
        steps:
          - run:
              name: auto-cancel
              command: |
                echo TODO

executors:
  default:
    docker:
      - image: circleci/buildpack-deps:latest

jobs:
  done:
    executor: default
    steps:
      - run: echo done

workflows:
  version: 2
  test:
    jobs:
      - auto-cancel/auto-cancel
      - approve:
          type: approval
      - done:
          requires:
            - approve
